[pytest]
# O CODEX_HOME do gateway fica em ./data (volume), e o Codex baixa para lá
# plugins que trazem testes próprios. Sem fixar o escopo, um `pytest` na raiz
# tenta coletá-los e quebra na colisão de nomes de módulo.
testpaths = tests
